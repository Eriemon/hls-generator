interface:
  display_name: "Readable HLS Generator"
  short_description: "Create, write, review, annotate, and validate readable HLS."
  default_prompt: "Use $readable-hls-generator to create, write, review, annotate, and validate readable HLS kernels and Vitis HLS workflows."
policy:
  allow_implicit_invocation: true
